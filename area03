print("โปรแกรมหาพื้นที่สี่เหลี่ยมจัตตุรัส")
z = float(input("ใส่ด้าน :"))
x = z*z
print(f"ค่าพื้นที่ของาี่เหลี่ยมจัตตุรัส = {x}")

print("โปรแกรมหาพื้นที่สี่เหลี่ยมผืนผ้า")
a = float(input("ใส่ความกว้าง : "))
b = float (input("ใส่ความยาว : "))
x =a*b
print(f"ค่าพื้นที่ของาี่เหลี่ยมผืนผ้า = {x}")

print("โปรแกรมหาพื้นที่วงกลม")
c= float(input("ใส่รัสมี : "))
g= 3.14 * (c**2)
print(f"ค่าพื้นที่ของวงกลม = {g}")